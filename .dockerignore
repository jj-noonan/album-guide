# The build needs the server and the API database. Everything else — the 143 MB
# crawl database, node_modules, the site build — would be uploaded on every
# deploy for nothing.
*
!api/server.py
!api/engine.py
!data/lexicon.json
!data/engine-constants.json
!data/catalog-api.sqlite
